# install
```
yarn add fetch-html
```
# usage
```
import fetch form 'fetch-html';
(async () => {
  // two arg
  // url
  // type text | html
  const result = await fetch('https://www.baidu.com', 'text');
})();
```
